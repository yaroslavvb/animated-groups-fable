# Search output: job lists, Modal batch logs, Float32 fields and per-job records,
# regenerated by modal_search.py. Kept out of the repository; the catalog builders read
# them from disk. compute-ledger.json and reservations.json are the accounting that stays.
*
!.gitignore
!reservations.json
